# Claude Code

This repository uses `AI_CONTEXT.md` as the official entry point for AI agents.

Begin every task by reading:

1. `AI_CONTEXT.md`
2. Only the documents referenced by `AI_CONTEXT.md` that are relevant to the current task.

Do not duplicate repository rules in this document.

`AI_CONTEXT.md` is the authoritative navigation entry point for this repository.