# Claude Code

This project uses `AI_CONTEXT.md` as the official entry point for AI agents.

Begin every task by reading:

1. `AI_CONTEXT.md`
2. The documents referenced by `AI_CONTEXT.md` that are relevant to the current mission.

Do not duplicate or redefine project rules in this document.

`AI_CONTEXT.md` is the authoritative navigation entry point for this project.